services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./server/api:/var/www/html/api
      - ./server/config:/var/www/html/server/config:ro
      - ./nginx.conf:/etc/nginx/sites-available/default:ro
    environment:
      - DB_HOST=localhost
      - DB_NAME=kuro_app
      - DB_USER=root
      - DB_PASS=
    restart: unless-stopped
